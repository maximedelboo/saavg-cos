# large generated artifact (reproduced by scripts/prepare_im_lookup.py, ~750 MB)
data/im_lookup_cos.zarr/

# NOTE: figures/*.png ARE committed -- they are the report's figures and are
# regenerated by scripts/*figure*.py / make_*.py. Keeping them in the repo lets
# the report and README render without first running the (MC-heavy) scripts.

# macOS
.DS_Store
**/.DS_Store

# python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
build/
dist/

# latex build products
report/*.aux
report/*.log
report/*.out
report/*.bbl
report/*.blg
report/*.synctex.gz
report/*.toc
report/*.fdb_latexmk
report/*.fls
